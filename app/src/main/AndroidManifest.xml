<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ico"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        tools:targetApi="33">
        <activity
            android:name=".UI_activities.ExercisesActivity"
            android:exported="false" />
        <activity
            android:name=".UI_activities.MusclesGroupsActivity"
            android:exported="false" />
        <activity
            android:name=".UI_activities.SetActivity"
            android:exported="false" />
        <activity
            android:name=".UI_activities.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".UI_activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>